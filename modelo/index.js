const casasAlimentacion = require('./casasAlimentacion');
const almacenes = require('./almacenes');
const productos = require('./productos');
const movimientos = require('./movimientos');
const existencias = require('./existencias');

module.exports = {
    casasAlimentacion,
    almacenes,
    productos,
    movimientos,
    existencias
};